# =============================================================================
# .gitignore do projeto lotgd
# -----------------------------------------------------------------------------
# Este arquivo impede que artefatos locais e dependências sejam versionados.
# Os comentários estão em PT-BR conforme as diretrizes do AGENTS.md do projeto.
# =============================================================================


# -----------------------------------------------------------------------------
# Artefatos de tempo de execução do Go
# -----------------------------------------------------------------------------
# Binários compilados manualmente (fora do go build padrão).
*.exe
*.exe~
*.dll
*.so
*.dylib

# Binário de teste gerado por `go test -c`.
*.test

# Perfis de cobertura e artefatos correlatos.
*.out
coverage.*
*.coverprofile
profile.cov


# -----------------------------------------------------------------------------
# Banco de dados SQLite e seus arquivos auxiliares de WAL
# -----------------------------------------------------------------------------
# O arquivo principal de save (lotgd.db) e seus artefatos de Write-Ahead Log
# são gerados dinamicamente em tempo de execução e NUNCA devem ser commitados.
*.db
*.db-journal
*.db-wal
*.db-shm


# -----------------------------------------------------------------------------
# Chave privada do servidor SSH (Wish)
# -----------------------------------------------------------------------------
# A chave de host (.wish_host_key) é material criptográfico sensível. Jamais
# deve ser versionada — em caso de comprometimento, regenere e distribua por
# canal seguro.
.wish_host_key
*.wish_host_key


# -----------------------------------------------------------------------------
# Diretórios de dependências
# -----------------------------------------------------------------------------
# vendor/ é comentado por padrão: habilite apenas se usar explicitamente.
# vendor/

# Arquivos de workspace do Go (multi-module).
go.work
go.work.sum


# -----------------------------------------------------------------------------
# Arquivos de ambiente
# -----------------------------------------------------------------------------
.env
.env.local


# -----------------------------------------------------------------------------
# Editores e IDEs
# -----------------------------------------------------------------------------
.idea/
.vscode/
*.swp
*~
*.swo


# -----------------------------------------------------------------------------
# Metadados do sistema operacional
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini


# -----------------------------------------------------------------------------
# Logs e dumps locais
# -----------------------------------------------------------------------------
*.log
*.dump
